spring:
    application:
        name: share-apply
    config:
        import: "configserver:"
    cloud:
         config:
            discovery:
                enabled: true
                service-id: "SERVICE-CONFIG"
            fail-fast: true
